<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Arranjo de verificacao: compila os arquivos da extensao que nao dependem de UI e os exercita
    numa pasta temporaria. Roda com "dotnet run" a partir desta pasta; sai com codigo 1 se algo
    falhar. Nao entra no .pext e nao e tocado pelo CI de release.
  -->

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net48</TargetFramework>
    <LangVersion>9.0</LangVersion>
    <RootNamespace>OrganizerTests</RootNamespace>
    <AssemblyName>OrganizerTests</AssemblyName>
    <PlayniteDir>D:\Playnite</PlayniteDir>
    <SrcDir>$(MSBuildThisFileDirectory)..\..\GameCaptureOrganizer</SrcDir>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
    <!-- Só para alcançar KeyInterop e HwndSource, de que o GlobalHotkey depende. Nenhuma janela é
         aberta aqui: o que se exercita é a tradução do nome da tecla, que é lógica pura. -->
    <UseWPF>true</UseWPF>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="Program.cs" />
    <Compile Include="$(SrcDir)\CaptureCore.cs" />
    <Compile Include="$(SrcDir)\SessionIndex.cs" />
    <Compile Include="$(SrcDir)\OrganizerService.cs" />
    <Compile Include="$(SrcDir)\OrganizerSettings.cs" />
    <!-- Da camada de captura automatica entra so o que e logica pura. O gatilho do Game Bar
         (SendInput) e o agendador dependem do Windows em execucao e nao cabem aqui. -->
    <Compile Include="$(SrcDir)\AutoCapture\CaptureTrigger.cs" />
    <Compile Include="$(SrcDir)\AutoCapture\TriggerLog.cs" />
    <Compile Include="$(SrcDir)\Gallery\GalleryScanner.cs" />
    <Compile Include="$(SrcDir)\Achievements\SteamKeyValues.cs" />
    <Compile Include="$(SrcDir)\Achievements\SteamStats.cs" />
    <!-- O atalho depende de janela do Windows, mas a tradução do nome da tecla é pura — e é
         justamente ela que, errada, roubaria o atalho de outro programa. -->
    <Compile Include="$(SrcDir)\Input\GlobalHotkey.cs" />
    <Compile Include="$(SrcDir)\AutoCapture\GameRules.cs" />
  </ItemGroup>

  <ItemGroup>
    <Reference Include="Playnite.SDK">
      <HintPath>$(PlayniteDir)\Playnite.SDK.dll</HintPath>
      <Private>true</Private>
    </Reference>
    <Reference Include="Newtonsoft.Json">
      <HintPath>$(PlayniteDir)\Newtonsoft.Json.dll</HintPath>
      <Private>true</Private>
    </Reference>
  </ItemGroup>

</Project>
